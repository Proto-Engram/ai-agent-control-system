# DEMO-OUTPUT-CONTRACT-v1.0

**Status:** Demo fixture — maps to `schemas/p1-output-contract.schema.json` for demonstration

Required top-level fields mirror the schema used in control-system tests.
See `tests/fixtures/output/` for valid and invalid examples.
